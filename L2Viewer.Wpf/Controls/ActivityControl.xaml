<UserControl x:Class="L2Viewer.Wpf.Controls.ActivityControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <StackPanel Margin="0,10,0,0">
        <StackPanel x:Name="ActivityPanel" Visibility="Collapsed">
            <ProgressBar Height="6" IsIndeterminate="True" />
            <ItemsControl x:Name="ActiveOperationsItemsControl" Margin="0,6,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="0,0,0,2" FontSize="11" Text="{Binding Description}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        <TextBlock x:Name="StatusTextBlock" Margin="0,4,0,0" TextWrapping="Wrap" FontSize="11" Text="" />
    </StackPanel>
</UserControl>
